# Implementation log

- Built the static App Router editorial story first, using a versioned Zod manifest and bundled deterministic coastal demo.
- Added recursive ingest, hash-keyed working assets, EXIF/GPS extraction, macOS HEIC fallback, technical scoring, perceptual duplicate grouping, contact sheets, OpenAI Structured Outputs, selection, approximate location inference, enrichment, narrative generation, metadata-free publishing, reports, and privacy scanning.
- Kept providers small and optional: deterministic demo mode is offline; enrichment degrades gracefully; strict people exclusion fails closed without vision credentials.
- Added unit, schema, selection, privacy, static-build, desktop, and mobile browser validation.
